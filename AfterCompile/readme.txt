These files can be used to that the user doesn't get confused when they dont see an applicaiton file (exe).
It will also run the program without a disabled terminal.

Just drag them into the directory.